# ADRs

Store architecture decision records here as implementation choices harden beyond the roadmap.

The roadmap remains the active planning document. Add ADRs here when a decision needs a durable rationale that should outlive a slice note.
